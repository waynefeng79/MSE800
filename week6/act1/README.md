# decorateor usage in the project
This project shows a simple decorator example which includes a wrapper onto users functions. When application calls those functions, the wrapper will be called instead.
The wrapper logs the activity of a function and call it as well, printing its name and execution time.
